'use strict';

const title = 'hello';
const object2 = {name: 'Bob', age: 12};

console.log(title);
console.warn(title);
console.error(title);

// Use a table to represent Object.
console.table(object2);